<div class="shopping-cart">
  <h2>Shopping Cart</h2>

  <div class = "p-detail">
    <span id = "c-heading">Product</span>
    <span>Quantity</span>
    <span>Price</span>
    <span>Total</span>
    <span>Actions</span>
  </div>
  <% @cart.cart_items.each do |item| %>
    <%= render 'cart_items/cart_item', item: item %> 
  <% end %>
</table>
</div>


<div class="outer">
  <span class="chechout">
    <%= link_to "Proceed to Checkout", checkout_index_path, method: :get, class: "checkout-link" %>
  </span>
</div>